{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Relayer Paths",
  "description": "Path metadata to relay packets between two different chains.",
  "type": "object",
  "required": [
    "chain_name",
    "paths"
  ],
  "properties": {
    "chain_name": {
      "type": "string"
    },
    "paths": {
      "type": "array",
      "items": [
        {
          "type": "object",
          "required": [
            "path_name",
            "src",
            "dst"
          ],
          "properties": {
            "path_name": {
              "type": "string",
              "description": "A non-dependant name. Usually the combination of two abbreviated chain names. EG: 'hubosmo' which can represent cosmoshub -> osmosis"
            },
            "src": {
              "$ref": "#/$defs/path_meta_data"
            },
            "dst": {
              "$ref": "#/$defs/path_meta_data"
            }
          },
          "tags": {
            "type": "array",
            "items": {}
          }
        }
      ]
    }
  },
  "$defs": {
    "path_meta_data": {
      "type": "object",
      "required": [
        "chain-id",
        "client-id",
        "connection-id",
        "channel-id",
        "port-id",
        "order",
        "version"
      ],
      "properties": {
        "chain-id": {
          "type": "string",
          "description" : "The chain ID of the network."
        },
        "client-id": {
          "type": "string",
          "description" : "The client ID on the corresponding chain representing the other chain's light client."
        },
        "connection-id": {
          "type": "string",
          "description": "The connection ID on the corresponding chain representing a connection to the other chain."
        },
        "channel-id": {
          "type": "string",
          "description": "The channel ID on the corresponding chain's connection representing a channel on the other chain."
        },
        "port-id": {
          "type": "string",
          "description": "The IBC port ID which a relevant module binds to on the corresponding chain."
        },
        "order": {
          "type": "string", 
          "description": "Determines if packets from a sending module must be ORDERED or UNORDERED."
        },
        "version": {
          "type": "string",
          "description": "IBC Version"
        }
      }
    }
  }
}